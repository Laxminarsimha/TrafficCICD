

export interface BreadCrumb {
    label: string;
    url: Array<any>;
    disabled: boolean;
}
export interface xgDays {
    code: string;
    label: string;
    daycode: number;
    daycode_N: number;
    isSelected: Boolean;
    isWeekDay: Boolean;
    longlabel: string;
  }