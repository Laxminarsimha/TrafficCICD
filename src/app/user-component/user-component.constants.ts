import { environment } from 'src/environments/environment';
// @dynamic
export class UserComponentConstants {
    public static constants = {
        urls: {
            getAdvertiserItems: `${environment.copyUrl}Copy/GetAdvertiserItems`
        }

    }
}